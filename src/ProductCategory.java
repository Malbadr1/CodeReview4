/*An enum is a special "class" that represents a group of constants ( like final variables).
To create it use the enum keyword (instead of class or interface), and separate the constants with a comma.
 Note that they should be in uppercase letters
 */
public enum ProductCategory {

    T_Shirts, Trousers, Shirts, Jackets, Accessories

}